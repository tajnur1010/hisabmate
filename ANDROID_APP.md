# ফোন অ্যাপ (Android APK) — HisabMate

এই গাইড ধরে এগোলে আপনার পিসিতে **Android Studio, JDK বা Android SDK কিছুই
ইনস্টল করতে হবে না**। APK তৈরি হবে GitHub-এর সার্ভারে, আপনি শুধু ফাইলটা
ডাউনলোড করে ফোনে ইনস্টল করবেন।

---

## কী বানানো হচ্ছে

HisabMate-এর ওয়েব কোডটাই [Capacitor](https://capacitorjs.com) দিয়ে একটা আসল
Android অ্যাপে মোড়ানো হয়। `dist/` বিল্ডটা **APK-এর ভেতরেই** থাকে, অ্যাপের
নিজের WebView সেটা চালায়। ফলে:

- ইন্টারনেট ছাড়াই অ্যাপ **খোলে** — কারণ পুরো ওয়েব বিল্ডটা APK-এর ভেতরেই আছে
- **অ্যাকাউন্ট ছাড়া চালালে পুরো খাতা নেট ছাড়াই কাজ করে** (ফোনের নিজের
  IndexedDB-তে জমা হয়)। নিচের "নেট ছাড়া চালানো" অংশে বিস্তারিত আছে
- ক্লাউড অ্যাকাউন্টে ঢুকতে ও সেই ডেটা দেখতে নেট লাগবে
- হোমস্ক্রিনে নিজের আইকন, নিজের স্প্ল্যাশ স্ক্রিন, ফুলস্ক্রিন — ব্রাউজারের
  কোনো অ্যাড্রেস বার নেই
- Android-এর ব্যাক বাটন অ্যাপের ভেতরেই পেছনে যায়, প্রথম স্ক্রিন থেকেই বেরোয়
- স্টেটাস বার লাইট/ডার্ক থিম অনুযায়ী রং বদলায়

ওয়েবসাইট (`https://hisabmate-nu.vercel.app`) আগের মতোই চলবে — এতে কিছু বদলায়নি।

**অ্যাপের পরিচয়:** `com.hisabmate.app` (`capacitor.config.ts`-এ)। এটা ডিভাইস ও
Play Store-এ অ্যাপের স্থায়ী নাম — পরে বদলালে সেটা সম্পূর্ণ নতুন অ্যাপ হিসেবে
গণ্য হবে, তাই এটা আর বদলাবেন না।

---

## ধাপ ১ — নতুন প্যাকেজ ইনস্টল করে বিল্ড টেস্ট করুন

`package.json`-এ নতুন ডিপেন্ডেন্সি যোগ হয়েছে, তাই **প্রথমে ইনস্টল** — নাহলে
`npm run build` "Cannot find module '@capacitor/core'" এরর দেবে।

```bash
cd E:\WORK\hisabmate
npm install
npm run build
```

দুটোই সফল হলে ধাপ ২-এ যান। এরর এলে সেটা আগে ঠিক করুন।

---

## ধাপ ২ — কোড GitHub-এ পাঠান

আগে থেকেই GitHub রিপো থাকলে (Vercel যেটা থেকে ডিপ্লয় করে) শুধু পুশ করুন:

```bash
git add .
git commit -m "Add Android app (Capacitor) + cloud APK build"
git push
```

রিপো না থাকলে GitHub-এ একটা **নতুন প্রাইভেট রিপো** বানিয়ে:

```bash
git init
git add .
git commit -m "HisabMate"
git branch -M main
git remote add origin https://github.com/<আপনার-ইউজারনেম>/hisabmate.git
git push -u origin main
```

> `.env`, `node_modules`, `android/`, `dist/` — এগুলো `.gitignore`-এ আছে, তাই
> আপনার আসল কী কখনো GitHub-এ যাবে না।

---

## ধাপ ৩ — Supabase কী দুটো Secret হিসেবে দিন

অ্যাপটা আসল ডেটাবেজের সাথেই বিল্ড হবে, ডেমো ডেটা দিয়ে নয়। কী দুটো কোডে না
রেখে GitHub Secret-এ রাখা হয়।

রিপোতে গিয়ে **Settings → Secrets and variables → Actions → New repository
secret**, তারপর দুটো সিক্রেট যোগ করুন:

| নাম                      | মান                                        |
| ------------------------ | ------------------------------------------ |
| `VITE_SUPABASE_URL`      | Supabase → Settings → API → Project URL    |
| `VITE_SUPABASE_ANON_KEY` | একই পেজের **anon public** কী               |

একই পেজের **Variables** ট্যাবে একটা ভ্যারিয়েবলও যোগ করুন:

| নাম               | মান                              |
| ----------------- | -------------------------------- |
| `VITE_PUBLIC_URL` | `https://hisabmate-nu.vercel.app` |

> **কখনো `service_role` কী দেবেন না** — সেটা সার্ভারের কী, ফ্রন্টএন্ড অ্যাপে
> গেলে যেকেউ সব ডেটা পড়তে-লিখতে পারবে। শুধু `anon` কী।
>
> `VITE_PUBLIC_URL` কেন দরকার: অ্যাপের ভেতরের ঠিকানা `http://localhost`, যেটা
> কোনো ইমেইল অ্যাপ খুলতে পারে না। তাই "পাসওয়ার্ড রিসেট" মেইলের লিংক আসল
> সাইটে পাঠানো হয় — লিংক ফোনের ব্রাউজারে খুলবে, পাসওয়ার্ড বদলাবে, তারপর
> অ্যাপে নতুন পাসওয়ার্ডে লগইন।

---

## ধাপ ৪ — APK বানান

রিপোর **Actions** ট্যাব → বাঁ দিকে **Android APK** → ডানে **Run workflow** →
সবুজ **Run workflow** বাটন।

প্রথমবার ৮-১২ মিনিট লাগে (Gradle সবকিছু নামায়)। কাজ শেষ হলে রান পেজের নিচে
**Artifacts** অংশে `hisabmate-android` দেখাবে।

সিক্রেট দিতে ভুলে গেলে বিল্ড শুরুতেই লাল হয়ে স্পষ্ট মেসেজ দেবে — চুপচাপ
ডেমো-ডেটার APK বানাবে না।

---

## ধাপ ৫ — ফোনে ইনস্টল

1. `hisabmate-android` আর্টিফ্যাক্টটা ডাউনলোড করুন (একটা `.zip` নামবে)।
2. জিপ খুলুন → ভেতরে `app-debug.apk`।
3. ফাইলটা ফোনে নিন (USB কেবল, বা নিজেকে WhatsApp/Telegram-এ পাঠিয়ে, বা Google
   Drive-এ রেখে)।
4. ফোনে ফাইলটায় ট্যাপ করুন। Android বলবে "unknown apps ইনস্টল করা বন্ধ" —
   **Settings → Allow from this source** চালু করে ইনস্টল দিন।
5. হোমস্ক্রিনে জেড রঙের HisabMate আইকন বসে যাবে।

আপডেট দিতে চাইলে: কোড পুশ করে আবার workflow চালান, নতুন APK একইভাবে ইনস্টল
করুন — অ্যাপ আইডি একই থাকায় পুরোনোটার উপরেই আপডেট হবে, ফোনের ডেটা মুছবে না।

---

## নেট ছাড়া চালানো (অফলাইন)

অ্যাপে **দুই ধরনের সেশন** আছে, আর নেট না থাকলে দুটো একেবারে আলাদা আচরণ করে।
এটা পরিষ্কার জেনে রাখা দরকার — সেভ না হয়েও "সেভ হয়েছে" দেখালে দোকানের টাকার
হিসাব নষ্ট হয়, তাই অ্যাপ ওরকম মিথ্যা কখনো বলে না।

### ১) অ্যাকাউন্ট ছাড়া — সম্পূর্ণ অফলাইন

সাইন-ইন স্ক্রিনের নিচে **"অ্যাকাউন্ট ছাড়াই শুরু করুন"** বোতাম আছে। ওটা চাপলে:

- কোনো রেজিস্ট্রেশন লাগে না, নেটও লাগে না — সিম ছাড়া ফ্লাইট মোডেও চলে
- কাস্টমার, সাপ্লায়ার, লেনদেন, খরচ, পণ্য — সব ফোনের নিজের স্টোরেজে জমা হয়
- অ্যাপ বন্ধ করে-খুললে বা ফোন রিস্টার্ট করলেও ডেটা থাকে
- উপরে সিঙ্ক জায়গায় ফোনের আইকন দেখাবে ("এই ফোনেই সেভ আছে") — সবুজ সিঙ্ক টিক
  দেখাবে না, কারণ আসলে কোথাও কিছু আপলোড হচ্ছে না
- Settings → **সাইন আউট**/"গেস্ট মোড থেকে বেরোন" দিলে ডেটা মুছে যায় না; আবার
  "অ্যাকাউন্ট ছাড়াই চালিয়ে যান" চাপলেই পুরো খাতা ফিরে পাবেন

> **সাবধান:** এই মোডে ক্লাউডে কোনো কপি নেই। অ্যাপ আনইনস্টল করলে বা Android
> Settings থেকে অ্যাপের স্টোরেজ ক্লিয়ার করলে খাতা চিরতরে যাবে। তাই মাঝেমধ্যে
> **Settings → ব্যাকআপ ফাইল নামান** দিয়ে JSON ফাইলটা নিজের Drive/WhatsApp-এ
> রেখে দিন।

### ২) ক্লাউড অ্যাকাউন্টে — নেট লাগবে

ইমেইল-পাসওয়ার্ড দিয়ে ঢোকা সেশনের ডেটা Supabase-এ থাকে, ফোনে থাকে না। তাই:

- সাইন ইন, সাইন আপ, পাসওয়ার্ড রিসেট — নেট ছাড়া হবে না, আর স্পষ্ট বাংলা মেসেজ
  দেখাবে ("ইন্টারনেট সংযোগ নেই") — "কিছু একটা সমস্যা হয়েছে" বলে ঘোরাবে না
- নেট ছাড়া অ্যাপ খুললে **"ইন্টারনেট সংযোগ নেই"** স্ক্রিন আসবে, সাথে তিনটা
  পথ: আবার চেষ্টা করুন · অ্যাকাউন্ট ছাড়াই চালান · সাইন আউট
- নেট ফিরে এলে ডেটা নিজে থেকেই লোড হয়ে যাবে, কিছু চাপতে হবে না
- এই মোডে অফলাইনে এন্ট্রি জমা রাখার কোনো ব্যবস্থা (outbox) **নেই** — অফলাইনে
  কাজ করার আসল রাস্তা উপরের ১ নম্বরটা

দোকানে নেট প্রায়ই থাকে না? তাহলে **অ্যাকাউন্ট ছাড়াই** চালানোই সবচেয়ে ভালো,
আর নিয়ম করে ব্যাকআপ ফাইল নামিয়ে রাখুন।

---

## Supabase-এ একবার যা ঠিক রাখতে হবে

**Authentication → URL Configuration**-এ থাকতে হবে:

- **Site URL:** `https://hisabmate-nu.vercel.app`
- **Redirect URLs:** `https://hisabmate-nu.vercel.app/reset`

নাহলে অ্যাপ থেকে পাঠানো রিসেট-লিংক কাজ করবে না।

প্রতিটা ফেজের SQL মাইগ্রেশনও (`supabase/migrations/`) SQL Editor-এ চালানো
থাকতে হবে — নাহলে অ্যাপ টেবিল খুঁজে পাবে না।

---

## পরে যদি নিজের পিসিতেই বিল্ড করতে চান

Android Studio ইনস্টল করলে (JDK ১৭ সহ আসে) কমান্ড দুটোই যথেষ্ট:

```bash
npm run app:add    # একবারই — android/ ফোল্ডার বানায়
npm run app:icons  # আইকন + স্প্ল্যাশ বানায়
npm run app:apk    # বিল্ড + সিঙ্ক + APK
```

APK পাবেন `android/app/build/outputs/apk/debug/app-debug.apk`-এ।
`npm run app:open` দিলে প্রজেক্টটা Android Studio-তে খুলবে।

কোড বদলালে শুধু `npm run app:sync` চালালেই নতুন বিল্ড অ্যাপে ঢুকে যায়।

---

## Play Store-এ দিতে চাইলে

এখনকার APK-টা **debug** বিল্ড — নিজে ও পরিচিতদের ফোনে দেওয়ার জন্য ঠিক আছে,
কিন্তু Play Store নেবে না। Play Store-এর জন্য লাগবে:

1. একটা **release keystore** (`keytool` দিয়ে বানানো) — এটা হারালে অ্যাপ আর
   কখনো আপডেট দিতে পারবেন না, তাই ব্যাকআপ রাখুন। কখনো গিটে কমিট করবেন না।
2. `android/app/build.gradle`-এ signing config, তারপর `./gradlew bundleRelease`
   দিয়ে `.aab` ফাইল।
3. Google Play Console অ্যাকাউন্ট (একবারের ২৫ ডলার ফি), প্রাইভেসি পলিসি ও
   ডেটা-সেফটি ফর্ম পূরণ।

চাইলে পরে workflow-তে সাইনিং যোগ করে দিতে পারি — keystore-টা GitHub Secret-এ
রেখে (base64) release AAB সরাসরি বানানো যায়।

---

## সীমাবদ্ধতা (এখন যা নেই)

- **iOS/iPhone অ্যাপ:** Capacitor iOS সাপোর্ট করে, কিন্তু iOS বিল্ড করতে
  একটা Mac + Xcode লাগে। Windows থেকে সম্ভব নয়।
- **ফন্ট:** Space Grotesk / Hind Siliguri এখনো Google Fonts থেকে আসে। প্রথমবার
  নেট থাকলে ফন্ট নেমে ক্যাশে জমা হয়ে যায়, তারপর অফলাইনেও ঠিক দেখায়; কিন্তু
  একেবারে প্রথম চালুটাই যদি নেট ছাড়া হয় তবে সিস্টেম ফন্ট দেখাবে। ফন্টগুলো
  অ্যাপের ভেতরে ঢুকিয়ে দিলে এটাও থাকবে না (ছোট কাজ, চাইলে করে দেব)।
- **বারকোড স্ক্যানার:** ফেজ ২-এ আসছে। নেটিভ অ্যাপ থাকায় সেটা ফোনের আসল
  ক্যামেরা দিয়ে অনেক ভালো কাজ করবে — এটাই APK রুটে যাওয়ার বড় লাভ।
