/**
 * Demo/sample-data generator — REMOVED.
 *
 * HisabMate ships as a real app with no built-in demo data. The old
 * `buildSeed()` helper (which populated fake parties/transactions/expenses)
 * and its "Load demo data" entry point have been deleted so nothing can ever
 * inject fabricated records into a real shop's ledger.
 *
 * This file is intentionally left as an empty module. The `export {}` keeps it
 * a valid ES module under `isolatedModules`. Safe to delete entirely once the
 * build toolchain can remove files again.
 */
export {};
